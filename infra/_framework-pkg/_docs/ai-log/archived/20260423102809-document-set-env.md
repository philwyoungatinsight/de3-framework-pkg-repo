# document-set-env

**Date**: 2026-04-23  
**Plan**: `ai-plans/document-set-env.md`

## What changed

### `infra/_framework-pkg/_framework/_git_root/set_env.sh`

- Rewrote file header comment: explains the script's purpose, how to source it, and what
  it does on every source (export vars, create dirs, run config-mgr generate)
- Added section headers inside `_set_env_export_vars()`:
  - `--- Repository root paths ---`
  - `--- Framework tool paths ---`
  - `--- Runtime dynamic directories ---`
  - `--- Config package resolution ---`
  - `--- GCS state backend (3-tier lookup) ---`
- Added inline comments for all non-obvious variables (`_ANSIBLE_ROLES_DIR`,
  `_WRITE_EXIT_STATUS`, `_CONFIG_MGR`, `_PKG_MGR`, `_UNIT_MGR`, `_EPHEMERAL`,
  `_CLEAN_ALL`, `_FW_REPO_MGR`, `_EPHEMERAL_DIR`, `_WAVE_LOGS_DIR`, `_GUI_DIR`,
  `_CONFIG_DIR`, `_FRAMEWORK_DIR`)
- Updated `_CONFIG_MGR` comment to reflect its full CLI: `generate/get/set/set-raw/move`
- Explained the config-package resolution: why Python (bash can't parse YAML), env-var
  override wins over file
- Documented the 3-tier GCS bucket lookup with priority order in comments
- Added one-line doc comments on `_set_env_create_dirs` and `_set_env_run_startup_checks`
- Explained the `unset -f` cleanup at the end
- No logic changes — comments only

### `infra/_framework-pkg/_docs/config-overview.md`

- Rewrote from scratch as a developer landing page
- Fixed all broken links (every link in the old file was wrong)
- Added `set_env.sh` as the entry point section — it was completely absent before
- Explained both kinds of config: package config (unit params) vs framework config (tool settings)
- Documented the 3-tier `_framework_settings/` lookup with priority table
- Added `$_CONFIG_MGR` subcommands reference table (generate/get/set/set-raw/move)
- Fixed SOPS section: references only the real `.sops.yaml` at repo root
- Added cross-references to `framework/config-files.md` and `framework/package-system.md`
- Consumer-repo paths (`config/_framework.yaml`, `.sops.yaml`) are prose + code, not links
  (they cross the symlink boundary and cannot be linked from the framework repo)
