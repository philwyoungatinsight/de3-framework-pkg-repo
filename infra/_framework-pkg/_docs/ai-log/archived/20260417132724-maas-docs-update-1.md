# maas-docs-update-1: Consolidate and Update MaaS Docs

## What changed

Updated four MaaS `_docs/` files and one memory file to reflect changes from snafus 25–31.

### `infra/maas-pkg/_docs/machine-onboarding.md`
- Machine table: added `mgmt_wake_via_plug` info for ms01-02 (Tapo 192.168.2.105); clarified ms01-03 has a smart plug but no `mgmt_wake_via_plug`
- Expanded the `mgmt_wake_via_plug` description to explain the distinction: having a smart plug ≠ needing the flag; ms01-03 is the example
- Replaced "ms01-01 only" note with separate per-machine notes (ms01-01: firmware crash; ms01-02: standby loss; ms01-03: no flag needed)
- AMT NIC table: added per-machine notes on AMT reliability
- Troubleshooting table: added two Rocky 9 deployment rows (grub2-install wrapper from snafu-25; zzz_1_cloud_init_success from snafu-26)
- Smart plug intro line: clarified ms01-01/02/03 are `power_type: amt` with smart plug for bounce only; nuc-1 is `power_type: smart_plug`

### `infra/maas-pkg/_docs/maas-lifecycle-sanity.md`
- Smart Plug Standby Bounce section: added per-machine breakdown (ms01-01, ms01-02, ms01-03) with plug IPs and reason for flag/no-flag

### `infra/maas-pkg/_docs/maas-lifecycle-waves.md`
- New "Custom Commissioning Scripts" section: documents `maas-00-install-lldpd` (active), notes `21-fix-lldpctl` as historical/removed, explains multi-phase commissioning and why serial scripts may cause additional reboot cycles

### `infra/maas-pkg/_docs/maas-state-machine.md`
- Commissioning Script Exclusions section: added pointer to custom scripts doc and `configure-commissioning-scripts.yaml`; added note on `parallel: none` vs `parallel: any` semantics and phase splitting

### Memory (`feedback_smart_plug_amt_rule.md`)
- Updated "currently only ms01-01" → ms01-01 and ms01-02, with per-machine reasons; updated MEMORY.md index line

## Verification
- `grep "ms01-01 only"` → 0 matches in `_docs/`
- `grep "21-fix-lldpctl"` → only appears in `maas-lifecycle-waves.md` as a historical note (correct)
